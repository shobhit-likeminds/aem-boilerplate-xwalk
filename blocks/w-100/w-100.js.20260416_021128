import { createOptimizedPicture } from '../../scripts/aem.js';
import { moveInstrumentation } from '../../scripts/scripts.js';

export default function decorate(block) {
  // The block model is empty, so we just need to apply the classes from the original HTML.
  // The original HTML shows: <div class="w-100 pt-3 pt-sm-3" style="background: ;"></div>
  // The block already has 'w-100' from the block name.
  block.classList.add('pt-3', 'pt-sm-3');

  // Since there are no fields in the model, there are no children to process.
  // The block will remain an empty div with the correct classes.
}
