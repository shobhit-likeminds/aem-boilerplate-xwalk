export default function decorate(block) {
  // The block has no content, only a class to apply.
  // The original HTML shows 'spaceadder-padding-80' class.
  block.classList.add('spaceadder-padding-80');
}
